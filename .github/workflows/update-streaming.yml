name: Weekly Streaming Data Refresh

on:
  schedule:
    # Every Tuesday at 08:00 UTC (3am Central during CDT, 2am during CST)
    # Shifted from Monday so the episode (drops Monday night) is on the RSS feed in time
    - cron: '0 8 * * 2'
  workflow_dispatch: {}   # lets you trigger it manually from the Actions tab to test

permissions:
  contents: write   # needed so the workflow can commit updated JSON back

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install requests

      - name: Auto-add any episode missing from rewatchables.json
        env:
          TMDB_API_KEY: ${{ secrets.TMDB_API_KEY }}
        run: python add_missing_episodes.py

      - name: Fill in missing hosts from RSS feed
        run: python fill_hosts.py

      - name: Run streaming data refresh
        env:
          WATCHMODE_API_KEY: ${{ secrets.WATCHMODE_API_KEY }}
        run: python update_streaming.py --recent

      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add streaming_data.json rewatchables.json
          if git diff --cached --quiet; then
            echo "No changes — nothing to commit."
          else
            git commit -m "Automated weekly refresh: hosts + streaming data"
            git push
          fi
